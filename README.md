# SysAgent

# To Run this Jawn
1. Clone the repo
2. `cd` into it and create the venv
3. Activate the venv
4. Install the `requirements.txt` packages
5. Make sure your `.env` is good.
5. Run `sysadmin_agent.py`


# Todo
- Use corpus of plain text files for RAG instead of Strings in the code.
- Get Linux machine to test on that Abhi has access to.
- Skibidi Abhi